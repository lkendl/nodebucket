/*
============================================
; Title: nodebucket
; File Name: base-response.js
; Author: Professor Krasso
; Date: 24 August 2022
; Modified By: Laura Kendl
; Description: Contains the Mongoose models for Employee and itemSchema.
; Simplifies boiler-plate code with request/response design pattern.
; Repeat behaviors within the application (i.e., employee-api.js) are
; replaced with the single object generated by the BaseResponse class.
===========================================
*/

// Pass httpCode, a message and data within the class.
class BaseResponse {
  constructor(httpCode, message, data) {
    this.httpCode = httpCode;
    this.message = message;
    this.data = data;
  }

  // Create a single object that returns to the user interface.
  toObject () {
    return {
      'httpCode': this.httpCode,
      'message': this.message,
      'data': this.data,
      'timestamp': new Date().toLocaleDateString()
    }
  }
}

module.exports = BaseResponse;
